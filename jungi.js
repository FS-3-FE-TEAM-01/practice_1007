export function junGi(go) {
    const duration = 5000; 
    const startTime = Date.now(); 

    const intervalId = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId1 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId2 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId3 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId4 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId5 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId6 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId7 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId8 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId9 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId10 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId11 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId12 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId13= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId14= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId15= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId16= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId17= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId18= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId19= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId20= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId21= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId22= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId23= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId24= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId25= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId26= setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId27 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    const intervalId28 = setInterval(() => {
        if (Date.now() - startTime >= duration) {
            clearInterval(intervalId); 
            return;
        }
        for(let i=0; i<100;i++){
            go()
        }
        
    }, 150);
    
}